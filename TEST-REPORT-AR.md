# تقرير اختبار الإصدار v0.7.1

تاريخ الاختبار: 2026-09-23

## النتيجة
V0.7.1 PASS: wording, left user menu, profile/password, saved prescription view, backup restore, data rollback, RTL, no errors

## الوظائف المختبرة
- استبدال العبارة الجانبية بنظام إدارة عيادة الطبيب محمد بسام حايك.
- إزالة بطاقة المستخدم القديمة من أعلى الواجهة.
- وضع قائمة المستخدم في أعلى يسار الشاشة.
- تعديل الاسم الظاهر واسم المستخدم.
- تغيير كلمة المرور والتحقق من الدخول بالكلمة الجديدة.
- حفظ الوصفة وربطها بالمريض.
- عرض الوصفة المحفوظة من ملف المريض بكامل الدواء والجرعة والاستخدام والمدة والتعليمات.
- طباعة الوصفة المحفوظة.
- إنشاء نسخة احتياطية تشمل قاعدة البيانات والمرفقات.
- ظهور النسخ السابقة مع زر استعادة.
- تأكيد مزدوج قبل الاستعادة.
- إنشاء نسخة أمان تلقائية قبل الاستعادة.
- استعادة قاعدة البيانات والمرفقات.
- التحقق من حذف البيانات المضافة بعد النسخة وعودة بيانات النسخة المختارة.
- تسجيل الخروج تلقائياً بعد الاستعادة.
- ثبات الواجهة العربية والقائمة اليمنى وعدم وجود أخطاء JavaScript أو تمرير أفقي.

## ملاحظة أمان
الاستعادة متاحة للأدمن فقط. العملية تستبدل البيانات الحالية، لذلك تنشئ نسخة pre-restore تلقائياً قبل التنفيذ وتطلب تأكيداً صريحاً من المستخدم.
